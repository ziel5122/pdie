const actionButton_ = {
  cursor: 'pointer',
  cursor: 'hand',
};

const styles = {
  imageButton: {
    ...actionButton_,
    marginTop: '8px',
  },
  imageWrapper: {
    display: 'inline-block',
    textAlign: 'center',
    width: '40px',
  },
  ngineButton: {
    ...actionButton_,
    marginTop: '8px',
  },
  ngineWrapper: {
    display: 'inline-block',
    height: '40px',
    margin: 0,
    verticalAlign: 'top',
  },
};

export default styles;
