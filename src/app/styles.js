const actions = {
  alignItems: 'center',
  display: 'flex',
};

const headerText = {
  marginLeft: '8px',
};

const text = {
  fontSize: '24px',
};

const textImage = {
  ...text,
  color: 'darkgray',
};

const textNgine = {
  ...text,
  color: 'gray',
};

export default {
  actions,
  headerText,
  textImage,
  textNgine,
};
